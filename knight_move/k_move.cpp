#include <iostream>
#include <vector>

using std::vector;
using std::cin;
using std::cout;
using std::endl;



vector<int> way(int stro, int stol, vector<vector<int>> k){
    vector<int> str;

    str.push_back(stro);
    k.push_back();
}